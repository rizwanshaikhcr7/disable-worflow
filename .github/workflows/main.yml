name: disable workflow

on:
  push:
    branches: [ main ]


jobs: 
   disable:
       runs-on: ubuntu-latest

       steps:
         - name: Farzzeen 
           run: echo "Disable worflow"
           


